# Phase 7: 接口层 (PowerShell CLI) 与 模型接入层
# 更新于 2026-04-26

## 7.1 模型接入层
- [x] 创建文件：`src/clients/base.py` ✅
  - [x] 定义抽象基类：`BaseLLMClient` ✅
    - [x] 抽象方法：`chat(self, messages: List[dict]) -> str` ✅
    - [x] 抽象方法：`chat_structured()` ✅
    - [x] 抽象方法：`count_tokens()` ✅
    - [x] 抽象方法：`chat_with_tools()` ✅

- [x] 创建文件：`src/clients/openai_like.py` ✅
  - [x] 定义类：`OpenAILikeClient(BaseLLMClient)` ✅
    - [x] 方法：`__init__(self, model_name: str, config: dict)` ✅
    - [x] 方法：`chat(self, messages: List[dict]) -> str` ✅
      - [x] 逻辑：调用 HTTP 请求 ✅
      - [x] 逻辑：尝试解析返回的 JSON 字符串 ✅
      - [x] 容错：如果模型没返回 JSON，包装成默认情绪返回 ✅
    - [x] 支持 tool calling ✅

- [x] 创建文件：`src/clients/manager.py` ✅
  - [x] 定义类：`ClientManager` ✅
    - [x] 单例模式 ✅
    - [x] 多模型切换 ✅
    - [x] API Key 管理 ✅

## 7.2 PowerShell 交互器
- [x] 创建文件：`src/interface/ps_shell.py` ✅
  - [x] 定义类：`PowerShellInterface` ✅
    - [x] 方法：`__init__(self, engine_core: EngineCore)` ✅
    - [x] 方法：`print_status(self, session: ConversationSession)` ✅
      - [x] 交互：向 PowerShell 打印表格 (当前人设、记忆条数、Token 估算) ✅
    - [x] 方法：`run_repl(self)` ✅
      - [x] 逻辑：主循环 ✅
        1. 读取用户输入 `input("> ")` ✅
        2. 检测指令：
           - `/save` -> 调用 session_manager.save ✅
           - `/load` -> 调用 session_manager.load ✅
           - `/role` -> 调用 asset_manager.load_npc ✅
           - `/world` -> 调用 asset_manager.load_world ✅
           - `/emotion toggle` -> 开关情绪渲染 ✅
           - `/exit` -> 触发 memory_engine.finalize_session -> break ✅
        3. 非指令：
           - 调用 orchestrator.build_messages ✅
           - 调用 llm_client.chat ✅
           - 调用 session.append_message ✅
           - 打印结果 ✅
    - [x] 支持 Ctrl+C 中断 ✅
    - [x] 模型切换指令 `/model` ✅ (dev mode)

## 7.3 消息格式 Schema
- [x] 创建文件：`src/core/config/message_schemas.py` ✅
  - [x] 定义多种模型的 message schema ✅
  - [x] 处理不同模型的 role 差异 (user_system, group 等) ✅
